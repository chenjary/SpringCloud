server:
  port: 8088

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chenjary/SpringCloud/
          searchPaths: config-repo
          username:
          password:
      label: master